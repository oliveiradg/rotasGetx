import 'package:flutter/material.dart';
    
class OffAllPageThree extends StatelessWidget {

  const OffAllPageThree({ Key? key }) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Page Three'),
      ),
      body: Container(),
    );
  }
}