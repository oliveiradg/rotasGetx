import 'package:flutter/material.dart';
    
class OffPageThree extends StatelessWidget {

  const OffPageThree({ Key? key }) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}